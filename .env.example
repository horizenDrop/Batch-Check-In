APP_URL=https://your-app.vercel.app
WEBHOOK_URL=https://your-app.vercel.app/api/webhook
FARCASTER_HEADER=
FARCASTER_PAYLOAD=
FARCASTER_SIGNATURE=
# Optional Redis for persistent state (recommended for deploy)
REDIS_URL=
# Local dev fallback uses in-memory state if REDIS_URL is empty.
# Player identity for local MVP is x-player-id header / localStorage generated id.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
PAYMASTER_URL=
